# SASI Design System — notas de sync (claude.ai/design)

- **Projeto alvo:** `157e2542-4b5d-48fd-82c8-5f3f693011a6` ("SASI · Comando UTI Design System").
- **Origem do bundle:** este projeto. O `design-system/` do repo é um export dele (namespace `SASIComandoUTIDesignSystem_157e25`).
- **Forma:** off-script — bundle pré-compilado, sem package.json/storybook/dist. O sync espelha `design-system/` -> projeto (não há converter a rodar).
- **Sync 19-Jun-2026:** conteúdo do DS já idêntico (manifest conferido). Re-subidos os 80 arquivos (idempotente) e REMOVIDO clutter do projeto: pasta aninhada `design_handoff_comando_uti/**`, `IMPLANT.md` e o standalone HTML da raiz.
- **Preservados** (infra do app, fora do espelho): `support.js`, `.thumbnail`, `_ds_manifest.json`.
- **Anchor `_ds_sync.json`:** omitido (off-script) — próximo sync re-verifica tudo.
