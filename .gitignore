# === Segredos / ambiente (NUNCA commitar) ===
.env
.env.*
!.env.example
chaves_secretas.txt
**/.env
**/.env.local

# === Dependências ===
node_modules/
vendor/

# === Build / artefatos ===
dist/
build/
*.tsbuildinfo
*.map

# === Supabase local ===
supabase/.temp/
supabase/.branches/

# === Logs / temporários do SO ===
*.log
.DS_Store
*.bak

# === Design system — cache de ferramenta ===
.thumbnail
design-system/uploads/

# === Config local de IA (scratch) ===
**/.claude/settings.local.json
.github/workflows/ci.yml  # habilitar com: gh auth refresh -s workflow

# === Índice do repo (regenerável) ===
memory/sasi_index.db
memory/sasi_index.db.bak
memory/MAPA-SASI.md.worktrees/
.worktrees/
