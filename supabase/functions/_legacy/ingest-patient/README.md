# ingest-patient (legado)

Edge function do app mobile **uti-tracker** (caminho antigo). **Não está deployada**
no projeto SASI atual — ingest operacional é Claude→JSON→MCP.

Mantida aqui só como referência de auditoria (`ingest_audit_log`) e para diff
histórico. Não remover sem confirmar que o mobile não chama mais este endpoint.